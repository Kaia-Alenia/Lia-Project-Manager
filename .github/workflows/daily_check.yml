name: Lia Daily Check

on:
  schedule:
    - cron: '0 13 * * *' 
  workflow_dispatch: # ESTA LÍNEA ES LA QUE ACTIVA EL BOTÓN

jobs:
  run-lia:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del código
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11' # Versión actualizada para Lía

      - name: Instalar herramientas de Lía
        run: |
          pip install --upgrade requests beautifulsoup4 google-genai

      - name: Ejecutar a Lía
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python monitor.py
